export default function(state = null, action) {
    switch (action.type) {
        case "CHANGESTAGE":
            let new_state = 0;
            return new_state;
        case "RESET":
            // same as CHANGESTAGE
        case "FRAME":
      
    }
  
    return state;
}