export default function(state = null, action) {
    switch (action.type) {
        case "MOVETOTIMELINE":
            // do something
            return action.payload;
        case "TIMELINETOMOVE":
            // do something
      
    }
  
    return state;
}
  